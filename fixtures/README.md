# fixtures/

`pack-minimal/` is a schema-valid pack whose three `frames/*.dng` are **text placeholders** — they prove the manifest/sha256/verifier contract, not pixels. Real bursts for R04 come from `fetch-burst-ipol.sh` (gitignored output) and, later, from the owner's SM-S938x.
