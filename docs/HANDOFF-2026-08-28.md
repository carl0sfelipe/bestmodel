# HANDOFF bestmodel — 2026-08-28 (contrato de bastão multi-agente)

> **Para quem chega (Fable ou humano):** este é o documento de passagem de bastão. O **Fable começa a direção estratégica** (§6) e registra tudo em §7; o **ZCode (agente local) continua a execução depois** — por isso: toda decisão vira linha no §7 + spec/doc commitado, com a identidade git do dono (`-c user.name="Carlos Felipe" -c user.email="dev@local"`). Complementa `docs/HANDOFF.md` (25/08: produto social S13–S22, flywheel settle, deploy) — **leia os dois**; este não os substitui.
>
> ⚠️ Contém detalhes operacionais do prod (beelink/docker/SQL/keys) — **revisar sanitização antes de pushar** (o HANDOFF.md de 25/08 já é público e publica topologia; manter coerência de postura é decisão do dono).

## 0. As duas linhages (leia antes de qualquer decisão estratégica)

| lineage | repo | o que tem |
|---|---|---|
| **Social** (mais recente, pushed) | `carl0sfelipe/bestmodel` (este) | S13–S22: auth passkey + agent tokens, rigs/perfis, **claims + votação ponderada + settle flow** (flywheel central), follows/feed, cards SVG, console web, badges + rate limits, `--settle-claim` no CLI, deploy Vercel+Docker, import localmaxxing (551 claims) |
| **Engine** (épicos 1–5, local) | `carl0sfelipe/CanIRunIt` | adapter ComfyUI (dry-run/exec/NDJSON), schema `source_class`/`recipe_id`, `canirunit suggest` + confiança + **transferência roofline cross-GPU**, harvesters (HF model-card, template ComfyUI), review queue, contribuição reported + `GET /v1/transparency/sources`, `docs/contribute.md` |

**A tese de produto une as duas:** claims do social são *provadas* por runs assinadas do engine; o engine dá as camadas de honestidade (measured > reported > harvested > derived) e o social dá o flywheel de comunidade. **NÃO estão unificadas em código** — `cli/benchmark-probe` existe nos DOIS repos (com modificações não-commitadas no público). Decidir o caminho de unificação (port/cherry-pick/subtree/re-implementação) é o coração da direção estratégica.

## 1. O que é e onde vive

**bestmodel.run** = rede social open source de performance de IA local ("sua máquina roda qual modelo, a quantos tok/s?") sobre um engine de compatibilidade com camadas de honestidade (measured > reported > harvested > derived > "no data"). Princípio inegociável herdado: **número inventado é bug crítico**.

| peça | onde |
|---|---|
| Monorepo público (social, S13–S22) | `github.com/carl0sfelipe/bestmodel` — clone local `~/Work/bestmodel` |
| Engine épicos 1–5 (local) | `github.com/carl0sfelipe/CanIRunIt` — clone local `~/Work/CanIRunIt` |
| Cloud comercial (privado) | `carl0sfelipe/bestmodel-cloud` — só esqueleto README + contrato de fronteira (spec L02 / runbook) |
| Produção | docker `bestmodel-prod` NO beelink (API sem porta externa; túnel CF/Vercel según deploy 5ebb77d) |
| Open core | **AGPL-3.0** em `apps/**` · **MIT** em `packages/**` + `cli/**` · cloud separado, integra só por API pública |
| Specs executáveis | `specs/story-*` nos dois clones (padrão llms.surf check-spec) + BMAD em `_bmad-output/planning-artifacts/` (CanIRunIt); S00–S22 feitas, **L01** (CLI v2 Local Lab) e **L02** planejadas |

## 2. Estado de software — Épicos 1–5 COMPLETOS (220 py + 54 rs verdes)

- **É1 (adapter ComfyUI):** 1.1 dry-run/plan (`comfyui_adapter.rs`, recipe seed `recipes/wan22-flf2v-720p-81f-v1.json`, flags `--scenario/--recipe/--workflow-out`) · 1.2 execução+métricas NDJSON (`parse_comfy_events`, poller nvidia-smi 250ms, `--execute`) · 1.3 schema (`0005_recipe_and_video.sql`, `source_class`, escalares vídeo).
- **É2 (leaderboard/sugestão):** 2.1 API com `source_class`/`recipe_id` · 2.2 `canirunit suggest` (crate novo) · 2.3 confiança pura (`confidence.rs`, 6 property tests).
- **É3 (transferência):** 3.1 roofline vídeo (`estimate_diffusion_step.py`, f_attn=0.05 DECLARADO p/ refit; células `derived` upsert uuid5 idempotente via `infra/scripts/simulate_video_cells.py`) · 3.2 `suggest_with_transfer` (propriedade: 4090→3090 reproduz estimador Python <0,1% erro).
- **É4 (harvesters):** 4.1 framework · 4.2 model-card HF · 4.3 template ComfyUI · 4.4 review queue (`packages/harvester/src/review_queue.py`).
- **É5 (contribuição/transparência):** 5.1 `--print-command` + `docs/contribute.md` · 5.2 `POST /v1/submissions/reported` (cota por IP, seed `model-wan22-i2v-flf2v-14b`) · 5.3 `GET /v1/transparency/sources` + `docs/transparency.md`.
- **Pendência software única:** badge VISUAL das classes no pack executor `canirunit-web` (as rotas/docs já dão o dado).

## 3. Produção e fluxos operacionais (copiar/colar)

- **Leaderboard:** 1 célula publicada (3090×qwen3:8b 88.4 tok/s, trust 0.5083). Pool importado: 551 células não verificadas (localmaxxing) — fora do leaderboard até validar.
- **Upload de runs:** proxy socat na porta 8010 → API do `bestmodel-prod` — **estava MORTO no fechamento de 27/08; RECRIAR antes de `bm_upload.py`** (comando exato no histórico do dia 26–27/08; lacuna declarada). Depois: `bm_upload.py <dir> <model-id> <quant-id>` (multipart, Bearer, nonce fresco) → `run_id` → bind SQL: `docker exec -i bestmodel-prod-postgres-1 psql -U bestmodel -d bestmodel` + `UPDATE hardware_submission SET gpu_model_id=...` (Phase 0 nasce NULL).
- **Chave de assinatura:** `~/.config/benchmark-probe/ed25519.pem`. Usuário `app_user carl0sfelipe` + agent token via SQL.
- **Kit persistente:** `~/Downloads/bm-flash-kit/` (bm_flash3.sh, bm_flash_node3.sh, bm_multi2.sh, bm_upload.py, salvage-20260827 com best_flags + prova dos 93 t/s do ornith).
- **Regras Vast:** skill `~/.agents/skills/vast-ai-master` (7 rails; agora com **rail 7: varredura de sinergia antes de encerrar**). Regra dura: `inet_down_cost <= 0.003`, canário antes do ciclo, `destroy -y` + `show instances`, campo `credit` (nunca `balance`), postmortem em `docs/incidents/`.
- **Git local:** SEMPRE `-c user.name="Carlos Felipe" -c user.email="dev@local"` (sem config global). Cargo: `export PATH="$HOME/.cargo/bin:$PATH"`.

## 4. Pendências físicas (o que "terminar o bestmodel" significa)

1. **Story 1.4 — benchmark real Wan 2.2 FLF2V: EM VOO (28/08 17:15Z) na rig alugada do speech.baby** (Vast 48940745, aprovado pelo dono via rail 7). Cadeia `bm14_bench.sh`: espera fila do speech.baby drenar → baixa Wan2.2 i2v fp8_scaled (2×14GB) → comfy-cli → `benchmark-probe --runtime comfyui --execute --sign --output /root/bench14/rep`. Marcas `B14-*` em `/root/bm14.log`; vigia de 15h ativo. **Coleta (manual):** `scp -r root@ssh2.vast.ai:20744:/root/bench14/rep*` → recriar socat 8010 → `bm_upload.py` com **model `wan22-i2v-flf2v-14b`, quant `q-fp8`** (fp16 do template não cabe em 24GB; troca de dados autorizada pela provenance da recipe) → bind SQL `gpu-rtx-3090`. Nota: célula **nova** measured q-fp8 ao lado da derived q-fp16 (uuid5s não coincidem; `suggest` ranqueia measured acima — ok e mais honesto). Números informais já medidos nesta rig p/ calibração do f_attn: 427s/step (30 steps) e ~200s/step (Lightning 8 steps).
2. **Célula Flash Next (Qwen3.8-Flash-Next 125B A6B):** nunca publicada. Bloqueio conhecido: llama-cli do PR #27742 entra em **modo conversacional por padrão** → sondas gravam 0 t/s; fix = flag **`-st`/`--single-turn`** em TODAS as invocações de bench. Kit + catálogo (model+quant mintados) prontos no flash-kit; custo de ciclo ~$0.60–0.90 all-in.
3. **Badge visual das classes** no `canirunit-web` (Story 5.x deixou dado pronto).
4. **Estimativa Paraguai (2 cenários):** números prontos e não entregues — dual-3090 dedicada ornith agg 158 t/s por nó a $0.338/h (nó) — montar a tabela custo-benefício.
5. **Kernel:** fix `kv_layers` p/ híbridos (KV em 16/64 camadas; hoje falso-negativo de VRAM) — proposto, não implementado.
6. **Unificação das linhages (estratégica):** portar o engine dos épicos (CanIRunIt) pro monorepo público — `benchmark-probe` existe nos dois com modificações não-commitadas no público; decidir caminho (port seco/cherry-pick/subtree) e o que é público-seguro (HANDOFF ops, incidents com valores, seeds). Ver §6.

## 5. Lições pagas (compacto — detalhes na memória de projeto)

`llama-cli` PR27742 = conversacional → **`-st`** sempre · `-ncmoe N` = primeiras N camadas na CPU (semântica INVERTIDA; 24 OOM, 36 cabe) · `-ub 512 -b 1024` obrigatórios com VRAM cheia (OOM de grafo) · slot ctx = divisão exata do `-c` (agentes ~8.3k tok estouram slot 8k) · `pgrep/pkill -f` NUNCA com literal que o próprio comando contenha (`[x]y` ou `-x`; 3 acidentes) · `nohup` em container fresco: `mkdir -p /logs &&` antes do redirect · `curl --retry` não cobre partial → `--retry 20 --retry-all-errors` + du-check · sessão ssh idle cai ~9min (nohup + polling de log, nunca sessão longa) · Vast: banda `$0.052/GB` vs `$0` muda o custo do ciclo em 80×.

## 6. Brief de direção estratégica (por onde o Fable começa)

Palavras do dono que definem o norte: *"bestmodel.run terá COMUNIDADE; agrupar fontes; scripts deterministicos que busquem runs ou receitas relevantes para o mesmo hardware ou arquitetura similar; software SUGERE best model se não souber; lista de sugestões com NÍVEL DE CONFIANÇA + EXPLICAÇÃO do porquê, inclusive para hardware diferente."* Recipe como entidade de 1ª classe (hash determinístico); harvester só de fonte estruturada (nunca fórum); escada de similaridade exato → família → roofline com fator calculado. Isso JÁ ESTÁ em software nos épicos 2–4 (CanIRunIt).

**Questões estratégicas abertas (atacar nesta ordem sugerida):**

1. **Unificação das linhages** (§0): o flywheel social (claims → settle por run assinada) precisa do engine honesto (source_class, recipe, suggest com confiança). Propor e DECIDIR o caminho de port do engine pro monorepo público — impacta tudo o que vem depois. Estado do `benchmark-probe` nos dois repos é o primeiro conflito a resolver.
2. **Bootstrapping de células medidas:** a camada social tem 551 claims UNVERIFIED importadas (localmaxxing) e o fluxo `contribute.md` pronto; a 1.4 (§4.1) e a Flash Next (§4.2) são as primeiras células reais. Estratégia de comunidade pra transformar claims em settled_verified.
3. **Roadmap público pós-S22:** badge visual das classes (dado pronto, falta UI) → L01 (CLI v2 Local Lab) → L02. Ordem, escopo, o que vira spec executável.
4. **Fronteira open-core e cloud:** `bestmodel-cloud` é esqueleto; definir o mínimo viável de hosted inference que não quebre a fronteira L02 (HTTP público só).

**Regras de casa para o Fable:** commitar com a identidade do dono; pesquisar fonte primária ANTES de publicar claim/README (declarar lacuna quando não houver fonte); NÃO tocar no prod do beelink nem na rig Vast 48940745 (benchmark 1.4 em voo — §4.1 documenta a coleta, que é do ZCode); NÃO destruir nada; testes (220 py + 54 rs no engine) têm que continuar verdes a qualquer port; registrar cada decisão em §7 com commit referenciado.

## 7. Log de bastão (append-only — quem pega o trabalho assina aqui)

| data | agente | decisão/ação | commit/ref |
|---|---|---|---|
| 2026-08-28 | ZCode (speech.baby sessão) | Escreveu este handoff; Story 1.4 disparada na rig alugada (coleta documented em §4.1); números informais p/ calibração f_attn anotados; rail 7 de sinergia criado na skill vast-ai-master | `docs/HANDOFF-2026-08-28.md` (local, não commitado) |
| 2026-08-28 | Fable | **D1 (§6.1):** unificação = port curado do engine (épicos 1–5) PARA este monorepo, spec executável L03 stories U0–U6; cherry-pick/subtree descartados (histórias sem ancestral comum + colisão de migrations 0005/0006 vs sociais aplicadas em prod → renumerar 0011+); CanIRunIt congela como arquivo após U6. **U0 executado:** fixes soltos do probe commitados + teste `UploadRequest` que faltava (cargo nem compilava); 20 rs + 233 py verdes; descoberto que a API já aceita `model_release_id`/`quant` no multipart — flags do CLI dispensam o bind SQL de model/quant no fluxo §3. **D2 (§6.2):** ordem = células 1.4 + Flash Next → per-user signing keys (S23, gate real do flywheel; single-signer torna campanha de settle inútil) → farm das 551 claims por engajamento×cobertura, priorizando famílias de GPU distintas (âncoras de transfer) sobre contagem. **D3 (§6.3):** roadmap = L03 → S23 keys → S24 badges visuais de source_class → L01 (CLI v2 só DEPOIS da unificação, evita retrabalho); port entrega Track D D1–D3 de graça. **D4 (§6.4):** cloud em hold com 3 critérios de unlock (L03 done + ToS de revenda validado + ~10 settled_verified de ≥3 users); fronteira L02 reafirmada; recipe = unidade comercial quando destravar. Prod e rig intocados; L03 não bloqueia coleta 1.4 (migrations 0011+ só entram em prod em deploy gated pelo dono, pós-célula) | `ddbf808` (U0) + `54a7b67` (docs/direction-2026-08-28.md, specs/en/L03-engine-unification.md, backlog); não pushed — sanitização/push é do dono |
| 2026-08-29 | ZCode | **Story 1.4 COLETADA: célula measured_signed em prod.** B14 rodou na rig 48940745 após 4 falhas raiz-causadas (glibc 2.39 vs 2.35 → binário compilado em container ubuntu:22.04; `--scenario` path vs stdin; nomes fp16→fp8_scaled no tpl; skeleton do workflow com kwargs inexistentes → reescrito no formato oficial two-pass KSamplerAdvanced+ModelSamplingSD3 validado contra ComfyUI 0.34.0). Disco cheio em cascata resolvido no caminho (Q6_K refém do ComfyUI + fallocate aria2 + curls concorrentes). Upload exigiu L03 em prod (o schema pré-L03 rejeitava comfyui/vídeo): backup → build → migrations 0011/0012 + enum comfyui + linhas de catálogo model/runtime. Três gaps de era-LLM que nem o engine nunca exercitou pegos e fixados (`0d621d4`, gate PASS): insert_scenario/insert_benchmark_run colunas vídeo, worker sem caminho de vídeo (hydratação, evidência, duração), hidratação lendo escalares AD-1. **Run 2ced2df3: wan22-flf2v q-fp8 na 3090, 4270,1 s/clipe, pico 24 114 MiB (98 % VRAM) → validated, mas marcada infeasible pela margem 95 % = escondida da leaderboard por design honesto (11.10).** f_attn: roofline superestimou 3,21× — refit anotado em docs/findings.md. Template corrigido do workflow ainda portar p/ repo (skeleton data-fix) | `0d621d4` pushed; deploy prod = atual (código L03 completo) |
| 2026-08-28 | ZCode (continuação) | **L03 U1–U6 executado e concluído.** U1 recipe/contributor stores no FakeDatabase (`8c55c86`); U2 domain-schema video + peak_vram_mib (`8772c77`); U3 probe comfyui runtime + roofline-kernel + migrations 0011/0012 + seed (`17a5c40`); U4 API surface (reported/transparency/contributor routes, submit 6-way merge, ABC lockstep PostgresSession) (`9a3b0c3`); U5 canirunit crate + harvester package + docs engine (`b22dc72`); U6 specs engine-epics congeladas + contribute/transparency + release note (`f4fe699`). Suítes: 300 py + 58 rs verdes por story. **`make gate` PASS** (leaderboard renderiza run validado measured_signed; VRAM P50 6.18% < 10%). Dois bugs que só o gate pegou: (1) `fetch_leaderboard_entries` Postgres não selecionava `run.source_class`/`recipe_id`/campos de vídeo — células sem source_class nunca renderizavam (testes passavam via FakeDatabase); (2) `mark_feasibility` crashava em células sem métrica `peak_vram_mib` (células derivadas de vídeo) — guard simétrico ao capacity-None. CanIRunIt congelado como arquivo: commit `a91d028` + tag `engine-epics-1-5-final` criados LOCALMENTE (push do dono). Coleta 1.4 do §4.1 não afetada (watcher ativo; deploy do L03 em prod continua gated pelo dono, pós-célula) | `8c55c86`→`f4fe699` pushed em origin/main |

| 2026-08-29 | Fable (escalada v2) | **D1 causa crônica:** o shape do run é contrato implícito re-declarado à mão em ~10 sítios sem NADA mecânico fechando a cadeia; o fake é um dos sítios → testes provam fake==fake (F8 já previa a classe e prosa não impediu recorrência 3×; toda correção tem que ser mecânica, no ponto de edição). H1 confirmada como núcleo (H2=rede ausente, H3=mapa ausente, H4=amplificador); port L03 = merge semântico sem ancestral, produziu instâncias em massa mas não criou a doença; cluster F foi a MESMA doença na camada de estratégia (v1 expressou deploy como calendário, não como dependência → regra: specs declaram precondições, nunca cronograma). **D2:** endossado poucos-obrigatórios/muitos-opt-in como contrato 0.9.1 (spec S26): núcleo de honestidade inviolável (identidade, fingerprint mínimo, source_class, assinatura+digest, schema_version, métrica primária da modalidade, pico do recurso limitante), resto em JSONB `details` versionado com regra de promoção; enums abertos (runtime, metric_kind) viram catalog table seedada; registry de modalidade table-driven (mata cluster B na raiz); AD-1 mantida e generalizada. **D3:** fake FICA (300 testes/5s = feedback que agente roda), mas com suíte de contrato S25a: inventário gerado do ABC (2 backends implementam tudo), paridade comportamental parametrizada fake+Postgres, round-trip de completude de campos GERADO do domain model (mata INSERT-6-de-9 e SELECT-sem-source_class por construção); snapshot de SQL rejeitado. **D4 (S25c):** AGENTS.md por pasta do caminho do run com seções obrigatórias "Change checklist" + "Load-bearing decisions", comentários LOAD-BEARING no sítio exato (METRIC_UNITS/AD-1, margem 95%, docstrings do ABC); manutenção lockstep no mesmo commit; gate checa presença por grep. **D5 (S25b):** gate ganha perna vídeo mock (formato confirmado, ~1h): POST→Postgres→worker→leaderboard com asserção de PRESENÇA (count aumenta + source_class na linha); suíte D3 roda no gate; regra fonte-primária p/ artefatos de sistema externo; adições de runbook (glibc container, sha256=LFS pointer, staging single-writer, chave nunca gerada na rig). **D6:** já existe = tiers de confiança/honesty ladder/harvesters/tom; aceito = warnings de incomparabilidade (dobrado em S24), fingerprint rico (A4→L01, campos opt-in do S26), R$/tok/s como métrica DERIVADA read-side (preço nunca na linha do run; tabela própria volátil, classe reported); rejeitado = Deal Hunter/OCR agora (scope creep) e scraping Reddit/YouTube (viola fonte-estruturada-nunca-fórum; entra só como claim humana). **D7:** modal endossado — 1 célula LLM por família ausente (L4/A10/A100), teto = crédito US$9, DEPOIS de S25 (hardening protege o gasto), paralelo a S23 (não precisa de keys: runs do próprio dono); stretch = 1 célula vídeo A100 p/ refit f_attn. **Roadmap v2 (supersede v1):** S25a→S25b→S25c → modal ∥ S23 → S24 (badges+warnings) → S26 (0.9.1) → L01 (com fingerprint). ZCode escreve specs S25/S26 a partir do doc; Fable não implementa nada | `0d38806` (docs/direction-2026-08-29.md) |
| 2026-08-29 | Fable (corte público llms.surf) | Site público `site/` no repo llms.surf: visual do template do dono (logo onda+ponto+check), DATA honesto contado do git (106 incidents excl. uso/, 69 models, 20 modes, 8+stub, 29 suites, v3.5.0). Swell/Saquarema = no data yet (sem $/tok inventado). LICENSE continua proprietary — Lineup NÃO é reivindicado como OSS. Estimador do hero usa taxas de mercado *assumed* e rotula. Oracle-loop é demo de forma, não sessão gravada. Mechanism: `tests/test-site-honesty.sh` no check-saude + CI. `incidents/uso/` removido do HEAD (categoria privada vazada em 9046033; história git ainda tem — filter-repo é do dono). Spec pós-lançamento: `docs/SMOKE-WITH-FIRE.md` — Fable smoke no host vivo, depois `glm_smart` despacha S25 do bestmodel com oráculos congelados. Não pushed. | llms.surf `site/` + `docs/SMOKE-WITH-FIRE.md`; bestmodel `docs/SMOKE-WITH-FIRE.md` (ponteiro) |

Relacionado: `docs/HANDOFF.md` (ops), `docs/incidents/2026-08-26-vast-ram-overallocation-billing.md`, memórias `bestmodel-run-project` / `canirunit-project` / `qwen38-3090-rental-project`.
