[project]
name = "bestmodel-comfy-lab"
version = "0.1.0"
description = "Diffusion vertical probe pack: ComfyUI runtime probe, workflow analyzer, best-fit CLI"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["src"]
