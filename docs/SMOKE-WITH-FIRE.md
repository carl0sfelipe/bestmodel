# Pointer — llms.surf smoke-with-fire (2026-08-29)

After **llms.surf v1** is live on a public host, Fable dogfoods the
deployed site (not a screenshot), then `llms-surf` dispatches GLM
(`glm_smart`) to execute **S25** under frozen oracles.

Canonical spec: `~/llms.surf/docs/SMOKE-WITH-FIRE.md`

Do not start S25 implementation in a GLM session that has not frozen
those oracles. Direction remains `docs/direction-2026-08-29.md`.
