[workspace]
resolver = "2"
members = ["cli/canirunit", "cli/benchmark-probe", "third_party/argos-opt"]

# benchmark-probe voltou a ser membro (2026-09-18): sua dependencia
# argos-opt agora mora DENTRO do repositorio, em third_party/argos-opt.
#
# ATENCAO: aquele diretorio e uma reconstituicao clean-room da API do
# argos-opt ORIGINAL (tree 31feea6), que foi perdido com a maquina que o
# hospedava — repo git local, sem remote, sem copia recuperavel (busca
# documentada no backlog A11). Vendorizacao autorizada pelo dono como
# interregno para o clone publico buildar. Quando o tree original
# reaparecer (Time Machine 30-31/ago, maquina beelink): substituir o
# diretorio pelo original e retomar o plano de publica-lo como git/registry
# dep (backlog A11).
